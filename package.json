{
  "name": "ben-there-done-that",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check asset-paths.js && node --check game-core.js && node --check game.js && node --check rpg-core.js && node --check rpg-campaign.js && npm test",
    "build:visual-manifest": "python tools/build_godot_visual_manifest.py",
    "validate:visual-manifest": "python tools/build_godot_visual_manifest.py --check",
    "validate:assets": "python tools/validate_curated_asset_catalog.py && node tools/validate_npc_asset_readiness.js && npm run validate:visual-manifest",
    "serve": "python -m http.server 8080"
  }
}
